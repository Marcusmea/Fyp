<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcus.fyp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lock"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lock"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomePageActivity" >

        </activity>
        <activity android:name=".SelectDoor" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FrontDoorActivity" />
        <activity android:name=".MasterRoomActivity" />
        <activity android:name=".BedDoor1Activity" />
        <activity android:name=".MainDoor" />
        <activity android:name=".Notification" />
        <activity android:name=".NotificationMasterRoom" />
        <activity android:name=".ResetPasswordActivity" />

        <service
            android:name=".IService"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>